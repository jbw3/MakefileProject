{
    // Whether to sort filenames when adding them to the makefile
    "sort_filenames": true,

    // Whether to add each filename to a different line in the makefile
    "multiline_filenames": true,

    // Template makefiles. These are in Packages/User/MakefileProject
    "templates":
    {
        "C":
        {
            "filename": "makefile_c"
        },

        "C++":
        {
            "filename": "makefile_c++"
        }
    }
}
